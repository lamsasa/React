import React from "react";

const About = () => {

    return (
        <>
        <h1>introduce page</h1>
        <p>in using react router</p>
        <button onClick={()=>navigator(-1)}>back</button>
        </>
    );
};

export default About;
